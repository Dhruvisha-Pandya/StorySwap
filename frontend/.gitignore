# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Firebase credentials
frontend/src/firebase/firebase.js    # Explicitly ignore the file
firebase-config*.js                 # Wildcard to catch any variant
.env                                # Global .env (already covered, but reaffirmed)